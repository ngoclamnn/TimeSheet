<Window x:Class="TimeSheet.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views = "clr-namespace:TimeSheet.Wpf.Views" 
        mc:Ignorable="d"
        Title="{Binding Title}" Height="457.649" Width="800"
          DataContext="{Binding Main, Source={StaticResource Locator}}" WindowStartupLocation="CenterScreen" Closed="Window_Closed">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Height="27" Content="{Binding DisplayTotalHour}"    VerticalAlignment="Top"  Grid.Row="0" Margin="250,40,377,0"/>
        <Label Width="50" Height="27" Content="UserId:"  Grid.Row="0" Margin="23,11,719,36"/>
        <TextBox Name="lbUserId" Padding="3" Text="{Binding UserId}" Width="95"  Grid.Row="0" Height="23" TextWrapping="Wrap" Margin="0,12,619,0" VerticalAlignment="Top" HorizontalAlignment="Right"/>

        <DataGrid Grid.Row="1"  HeadersVisibility="Column" ItemsSource="{Binding TimeSheetInfos}" Name="dataGrid" Margin="10,10,10,10" AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Header="Weekday" Width="*" Binding="{Binding DisplayDayOfWeek}" >

                </DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="In" Width="*" Binding="{Binding osdTimeIn, StringFormat=t}" >

                </DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Out" Width="*" Binding="{Binding DisplayTimeOut}">

                </DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Total" Width="*" Binding="{Binding DisplayTotalHour}">

                </DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Missing" Width="*" Binding="{Binding DisplayMissing}">

                </DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Expected Out" Width="*" Binding="{Binding DisplayExpected}">

                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Label Height="27" Content="{Binding UserFullName}"  VerticalAlignment="Top"  Grid.Row="0" Margin="23,40,579,0"/>
    </Grid>



</Window>
